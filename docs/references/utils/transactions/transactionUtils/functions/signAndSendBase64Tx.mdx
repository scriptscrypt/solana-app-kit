---
title: "Function: signAndSendBase64Tx()"
description: "> **signAndSendBase64Tx**(`base64Tx`, `connection`, `provider`): `Promise`\<`string`\>"
---

[**solana-app-kit**](../../../../README.mdx)

***

[solana-app-kit](../../../../README.mdx) / [utils/transactions/transactionUtils](../README.mdx) / signAndSendBase64Tx

# Function: signAndSendBase64Tx()

> **signAndSendBase64Tx**(`base64Tx`, `connection`, `provider`): `Promise`\<`string`\>

Defined in: [src/utils/transactions/transactionUtils.ts:40](https://github.com/SendArcade/solana-social-starter/blob/b27156124ee1831d644b1af765e8d55a2885cd4f/src/utils/transactions/transactionUtils.ts#L40)

Takes a base64-encoded transaction (could be legacy or versioned),
deserializes it, and then uses Privy provider to sign+send.

## Parameters

### base64Tx

`string`

### connection

`Connection`

### provider

`any`

## Returns

`Promise`\<`string`\>
