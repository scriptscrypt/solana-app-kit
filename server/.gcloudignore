# .gcloudignore

# Exclude git directories and files
.git
.gitignore

# Exclude Node.js dependencies (they will be installed on Cloud)
node_modules/

# Exclude source files and TypeScript configuration (we pre-build locally)
src/
tsconfig.json
*.ts

# Exclude local environment file (manage these via Cloud Console)
.env

# Exclude log files (optional)
*.log
